"use client";import{useEffect,useState}from"react";export default function Obrigacoes(){const[data,setData]=useState([]);async function load(){const r=await fetch("/api/obligations");if(r.ok)setData(await r.json())}useEffect(()=>{load()},[]);async function status(id,value){await fetch("/api/obligations/status",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({id,status:value})});load()}return <main style={{padding:32}}><a href="/">← Painel</a><h1>Obrigações</h1><table cellPadding="12" style={{background:"white",width:"100%"}}><thead><tr><th>Empresa</th><th>Competência</th><th>Obrigação</th><th>Vencimento</th><th>Status</th></tr></thead><tbody>{data.map(x=><tr key={x.id}><td>{x.client.legalName}</td><td>{x.period}</td><td>{x.name}</td><td>{new Date(x.dueAt).toLocaleDateString("pt-BR")}</td><td><select value={x.status} onChange={e=>status(x.id,e.target.value)}><option value="PENDENTE">Pendente</option><option value="EM_ANDAMENTO">Em andamento</option><option value="CONCLUIDA">Concluída</option></select></td></tr>)}</tbody></table></main>}