import{NextResponse}from"next/server";import{cookies}from"next/headers";import{db}from"../../../lib/db";import{readSession,requireMaster}from"../../../lib/auth";export async function GET(){const s=readSession(cookies().get("unicon_session")?.value);if(!requireMaster(s))return NextResponse.json({error:"Somente Master"},{status:403});return NextResponse.json(await db.auditLog.findMany({take:100,orderBy:{createdAt:"desc"},include:{user:{select:{name:true,email:true}}}}))}