import{NextResponse}from"next/server";import{cookies}from"next/headers";import{db}from"../../../lib/db";import{readSession}from"../../../lib/auth";export async function GET(){const s=readSession(cookies().get("unicon_session")?.value);if(!s)return NextResponse.json({error:"Não autorizado"},{status:401});return NextResponse.json(await db.certificate.findMany({include:{client:true},orderBy:{expiresAt:"asc"}}))}export async function POST(){return NextResponse.json({error:"Upload bloqueado até configuração do cofre KMS/object storage"},{status:503})}