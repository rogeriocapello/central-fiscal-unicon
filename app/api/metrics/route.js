import{NextResponse}from"next/server";import{cookies}from"next/headers";import{db}from"../../../lib/db";import{readSession}from"../../../lib/auth";export async function GET(){const s=readSession(cookies().get("unicon_session")?.value);if(!s)return NextResponse.json({error:"Não autorizado"},{status:401});const [clients,certificates,obligations,pending,users]=await Promise.all([db.client.count({where:{active:true}}),db.certificate.count(),db.obligation.count(),db.obligation.count({where:{status:"PENDENTE"}}),db.user.count({where:{active:true}})]);return NextResponse.json({clients,certificates,obligations,pending,users})}