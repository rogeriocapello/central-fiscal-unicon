"use client";import{useState}from"react";import{useRouter}from"next/navigation";export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const r=useRouter();async function submit(e){e.preventDefault();setError("");const res=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password})});if(res.ok)r.push("/");else setError("Usuário ou senha inválidos.")}return <main style={{maxWidth:420,margin:"10vh auto",background:"white",padding:30,borderRadius:14}}><h1>Central Fiscal Unicon</h1><p>Acesso ao ambiente fiscal</p><form onSubmit={submit}><input style={{width:"100%",padding:12,margin:"8px 0"}} placeholder="E-mail" value={email} onChange={e=>setEmail(e.target.value)}/><input style={{width:"100%",padding:12,margin:"8px 0"}} type="password" placeholder="Senha" value={password} onChange={e=>setPassword(e.target.value)}/><button style={{width:"100%",padding:12,marginTop:12}}>Entrar</button>{error&&<p>{error}</p>}</form></main>}