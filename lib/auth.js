import jwt from "jsonwebtoken";export function signSession(user){return jwt.sign({sub:user.id,name:user.name,role:user.role},process.env.JWT_SECRET,{expiresIn:"8h"})}export function readSession(token){try{return jwt.verify(token,process.env.JWT_SECRET)}catch{return null}}export function requireMaster(session){return session?.role==="MASTER"}